version: 3

project:
  locator: SolaceProducts_terraform-provider-solacebroker
  id: SolaceProducts_terraform-provider-solacebroker
  name: terraform-provider-solacebroker
  teams: []
  labels:
    - go

vendoredDependencies:
  forceRescans: false
  scanMethod: CLILicenseScan
  licenseScanPathFilters:
    exclude:
      - "./.git"
      - "./.github"

paths:
  exclude:
    - ./.git
    - ./.github

telemetry:
  scope: full
